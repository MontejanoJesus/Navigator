<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solvd.navigator.dao.IDriverDAO">
    <resultMap id="driverResultMap" type="com.solvd.navigator.model.Driver">
        <id property="id" column="id"/>
  		<result property="name" column="name"/>
    </resultMap>

    <select id="getById" parameterType="long" resultMap="driverResultMap">
        SELECT * FROM Drivers WHERE id = #{id}
    </select>

    <select id="getAll" resultMap="driverResultMap">
        SELECT * FROM Drivers
    </select>

    <insert id="insert" parameterType="com.solvd.navigator.model.Driver">
        INSERT INTO Drivers (id, name) VALUES (#{id}, #{name})
    </insert>

    <update id="update" parameterType="com.solvd.navigator.model.Driver">
        UPDATE Drivers SET name = #{name} WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM Drivers WHERE id = #{id}
    </delete>
</mapper>